# Sapphire — pinned requirements (last updated 2026-03-28)
# To update: pip install --upgrade <package> && pip freeze > check, then update here.
# stripe and starlette held back — major version breaks (15.x and 1.x respectively).

# --- Core Framework ---
fastapi==0.135.2
pydantic[email]==2.12.5
pydantic-settings==2.13.1
python-multipart==0.0.22

# --- Database ---
SQLAlchemy[asyncio]==2.0.48
asyncpg==0.31.0

# --- Auth ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0

# --- HTTP Client (async) ---
httpx==0.28.1
aiohttp==3.13.4
requests==2.33.0

# --- Encryption ---
cryptography==46.0.6

# --- Logging ---
structlog==25.5.0

# --- Azure Communication Services (phone numbers, SMS, call automation) ---
azure-communication-phonenumbers==1.3.0
azure-communication-callautomation==1.4.0
azure-communication-sms==1.1.0

# --- Stripe (invoicing, subscriptions, billing) ---
stripe==14.4.1

# --- ngrok (local tunnel for webhook dev) ---
pyngrok==7.5.1

# --- Azure ---
azure-functions==1.23.0
azure-identity==1.25.3
azure-keyvault-secrets==4.10.0
azure-ai-projects==2.0.1
# msal 1.29+ required for UserAssignedManagedIdentity (federated credential, no client secret)
msal==1.35.1

# --- AI ---
openai==2.30.0

# --- Utilities ---
python-dotenv==1.2.2
anyio==4.13.0

# --- Testing ---
pytest==9.0.2
pytest-asyncio==1.3.0
